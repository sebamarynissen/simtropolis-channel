There's numerous packages that will end up utilizing variants meaning the same thing - e.g. options if you use maxis/tarsealed/westwind streets. should update this to utilize a single higher level variant instead of having each be at the package level but meaning the same thing.